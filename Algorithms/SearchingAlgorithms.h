#pragma once
#include <vector>

class SearchingAlgorithms
{
public:
	static int LinearSearch(std::vector<int> arr, int searchKey);
	static int BinarySearch(std::vector<int> arr, int searchKey);
};

